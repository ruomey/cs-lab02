#include <iostream>

using namespace std;

int main(){
    cout << "Enter A and B: ";
    int a, b;
    cin >> a >> b;
    cout << "a + b = "<< a + b << '\n'
    << "a - b = "<< a - b << '\n'
    << "a * b = "<< a * b << '\n'
    << "a / b = "<< a / b << '\n';
    return 0;
}
